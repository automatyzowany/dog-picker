require 'rails_helper'

RSpec.describe 'DogMatcher' do
  # it 'should return 100 in hash when all the answers are good' do
  #   poll = { '1': 1, '2': 2, '3': 3 }
  #   dog = create_list(:dog, 1)
  #   answers = create_list(:answer, 1)

  #   result = DogMatcher.new(poll, dog).call

  #   expect(result).to include('1' => 100)
  # end
end
